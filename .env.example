# Online multiplayer (optional — without these the game is single-player only).
# See supabase/README.md for setup.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Web-push turn alerts (optional; needs the turn-push edge function deployed).
VITE_VAPID_PUBLIC_KEY=
